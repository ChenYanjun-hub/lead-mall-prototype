# 头等仓线索商城改版原型 · 规格说明

> 本文件是这个原型的**唯一事实来源**。任何需求变更先改本文件，再改代码。
> 代码与本文件不一致时，以本文件为准。

---

## 0. 项目背景

### 0.1 这是什么

头等仓（工业及物流地产获客平台）微信小程序中「线索商城」模块的**改版方案原型**，用于产品面试交付。

线索商城的业务模型：平台自行获客拿到租户的找仓需求 → 客服电话核实并录音 → 上架为「客户线索」商品 → 业主/经纪人用积分认领 → 认领后可见客户手机号，自行推进带看与签约。

- **付费方**：业主、经纪人（主力为中小经纪人与二房东）
- **被交易方**：租户企业（免费提需求，不是本产品的用户）
- **平台收入**：仅来自积分销售，不参与成交佣金分成

### 0.2 核心论点（所有设计的出发点）

现有产品**已经解决了「信任」**——平台电话核实、完整通话逐字稿凭证、无效退积分，这套机制在行业内领先。

但它**没有解决「分辨」**：

- 平台已按客户规模分三档定价（普通 50 / 大客户 100 / 超大 200 积分）
- 但成交收佣 = 面积 × 39.36，也是面积的线性函数
- **两个价格用的是同一个维度，成交概率在整个定价模型里被当成了常数**

因此改版目标不是拉高认领率，而是**提升每积分的成交转化率**。

三条主线：

| 主线 | 解决的问题 | 具体改动 |
|---|---|---|
| 让好线索卖出好价 | 定价分层用错维度 | 规模档 × 质量系数、信任标识前置、席位上限、时效衰减 |
| 让信任资产被读完 | 通话凭证被埋在信息过载里 | AI 通话摘要卡、逐字稿折叠、字段结构化抽取 |
| 让数据闭环合上 | 回填只收正样本 | 负样本回填、CRM 数据接回评分、申诉入口前移 |

### 0.3 硬约束

| 约束 | 说明 |
|---|---|
| **单文件** | 交付物为一个 `.html`，内联全部 CSS 与 JS，不拆模块、不引入构建工具 |
| **零外部依赖** | 不引用任何 CDN、字体、图片。所有图形用内联 SVG 或 emoji |
| **纯静态可部署** | 拖进 Vercel / Cloudflare Pages 即可出链接，无需服务端 |
| **无浏览器存储** | 禁止 localStorage / sessionStorage。状态只存在内存中，刷新即重置 |
| **中文界面** | 全部文案为简体中文 |
| **移动端可用** | 必须声明 `color-scheme` 阻止浏览器强制深色反转；窄屏用 `transform: scale` 缩放手机，不得横向溢出。详见 10.11 |
| **无障碍基本要求** | 保留 `:focus-visible` 样式与 `prefers-reduced-motion` 处理 |

---

## 1. 页面总体结构

单个 HTML 页面，从上到下分为四层：

```
┌─────────────────────────────────────────────┐
│ masthead        标题 + 核心论点 + 元信息标签   │
├─────────────────────────────────────────────┤
│ navtabs         8 个分区按钮（两排，可换行）    │
│ navhint         一行说明文字                  │
├─────────────────────────────────────────────┤
│ stage           手机原型（左）+ 说明面板（右）  │  ← 分区 02–05
│   或                                         │
│ analysis        全宽分析内容                  │  ← 分区 00/01/06/07
├─────────────────────────────────────────────┤
│ foot            数据来源与免责说明             │
└─────────────────────────────────────────────┘
```

### 1.1 顶部分区导航（8 个按钮）

| 序号 | 名称 | 类型 | 行为 |
|---|---|---|---|
| 00 | 业务结构 | 分析屏 | 显示 `#a-model`，隐藏 stage |
| 01 | 用户定位 | 分析屏 | 显示 `#a-segment` |
| 02 | 线索列表 | 可交互 | 显示 stage，`resetTo('mall')` |
| 03 | 需求详情 | 可交互 | 显示 stage，栈设为 `[mall, detail/215301]` |
| 04 | 新手引导 | 可交互 | 显示 stage，`resetTo('mall')` 并 `onboard = 1` |
| 05 | 认领后跟进 | 可交互 | 显示 stage，若未认领则自动认领 215301（扣分、席位+1、status=1），栈设为 `[mine, claimed/215301]` |
| 06 | 改版对比 | 分析屏 | 显示 `#a-compare` |
| 07 | 落地方案 | 分析屏 | 显示 `#a-plan` |

- 默认选中 **02**
- 可交互分区带绿色 `可交互` 徽标
- 切换分区后 `window.scrollTo({top:0})`
- **禁止**给这个导航条加 `position:sticky` 或底部渐变遮罩——曾因此导致第二排按钮被遮住

### 1.2 手机外观

- 外框 390 × 800，圆角 46px，内边距 11px，黑色机身
- 屏幕圆角 36px，背景 `--bg`
- 竖向 flex：状态栏(40) → 导航栏(44，首页无) → viewport(flex:1，滚动) → dock(条件渲染) → tabBar(56)
- 微信胶囊按钮绝对定位于 `top:48px; right:16px`
- 手机下方有两个控制按钮：`重置原型`、`重看新手引导`

---

## 2. 设计 Token

### 2.1 颜色

```css
--brand:#3D5AFE   --brand-deep:#2B45D8  --brand-soft:#EEF1FF  --brand-line:#C6D0FF
--orange:#FF6B35  --orange-soft:#FFF1EA
--green:#16A97A   --green-soft:#E8F7F1
--gold:#B8860B    --gold-soft:#FDF5E3
--red:#E05B5B     --red-soft:#FDEEEE
--amber:#E8A33D   --amber-soft:#FCF3E3
--bg:#F2F4F8      --card:#FFF
--ink:#17181C     --ink2:#5B6069  --ink3:#9AA0A9  --line:#EDEFF3
/* 页面外壳（深色） */
--page:#0E1116    --page2:#161A21 --page-line:#252B35
```

语义约定：

- 蓝 = 品牌与主操作　橙 = 收益金额　绿 = 已核实/成交/正向
- 金 = 积分　红 = 风险与警示　琥珀 = 需注意但不severe

### 2.2 质量等级配色

| 等级 | 分数区间 | 文字色 | 底色 |
|---|---|---|---|
| A | ≥ 85 | `--green` | `--green-soft` |
| B | 60–84 | `--amber` | `--amber-soft` |
| C | < 60 | `--red` | `--red-soft` |

### 2.3 字体与圆角

- 字体栈：`"PingFang SC","Hiragino Sans GB","Microsoft YaHei",-apple-system,BlinkMacSystemFont,sans-serif`
- 手机内卡片圆角 14px，小组件 9–11px，弹层 20px（仅上方两角）
- 手机内正文 12.5–13px，标题 14–19px，标签 10.5px

### 2.4 类名冲突约定（重要）

手机内组件与页面分析屏的类名**曾经冲突**，已用前缀区分，重构时必须保持：

| 手机内 | 分析屏 | 说明 |
|---|---|---|
| `.sec-h` | `.an-h` | 小节标题 |
| `.tabbar`（手机底栏） | `.navtabs`（页面分区） | 两者都是"标签栏"，绝不可合并 |
| — | `.analysis table` | 分析屏表格样式必须带 `.analysis` 前缀，避免污染手机内表格 |

---

## 3. 数据模型

### 3.1 常量

```js
const FEE = 39.36;                 // 成交收佣系数（线上真实值，勿改）
const CITIES = ['上海','苏州','南京'];
const CITY_TOTAL = { 上海:408, 苏州:121, 南京:190 };   // 展示用总数，与实际收录条数无关
const STATUS = ['待联系','已联系','已带看','已成交','已战败'];
```

### 3.2 仓源 STORES（共 6 个，代表"我"名下的房源）

| id | name | city | district | area | h（层高 m） |
|---|---|---|---|---|---|
| s1 | 长征路 128 号库 | 上海 | 普陀 | 130 | 5.2 |
| s2 | 宜川北路 6 号库 | 上海 | 普陀 | 145 | 4.8 |
| s3 | 嘉定南翔库 | 上海 | 嘉定 | 300 | 6.0 |
| s4 | 漕河泾恒温库 | 上海 | 徐汇 | 320 | 4.5 |
| s5 | 松江九亭大库 | 上海 | 松江 | 2200 | 9.0 |
| s6 | 昆山花桥库 | 苏州 | 昆山 | 120 | 5.0 |

### 3.3 线索 LEADS（共 14 条）

字段定义：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | string | 线索编号，与线上格式一致（6 位数字） |
| `city` / `district` | string | 城市 / 区域。`district` 可为 `'不限'` |
| `type` | `'厂房'｜'仓库'` | 用于类型筛选 |
| `area` | number | 用于计算收佣与规模档，取需求下限 |
| `areaTxt` | string | 展示文案，如 `'100-150平'` |
| `use` | string | 用途 |
| `grade` | `'A'｜'B'｜'C'` | 质量等级 |
| `score` | number | 质量分 0–100 |
| `verified` | bool | 平台是否已电话核实 |
| `record` | bool | 是否有通话逐字稿凭证 |
| `urgent` | bool | 需求是否紧急 |
| `nonAgent` | bool | 是否已确认非中介 |
| `complete` | number | 信息完整度百分比 |
| `days` | number | 已上架天数 |
| `viewers` | number | 正在查看人数 |
| `seats` / `taken` | number | 认领席位上限 / 已认领数 |
| `match` | number | 与我仓源的匹配度 0–100 |
| `stores` | string[] | 匹配到的仓源 id |
| `phone` | string | 客户手机号（含空格分组） |
| `desc` | string | 需求描述 |
| `ai` | object? | 通话摘要，**仅 `record:true` 的线索有** |
| `spec` | array | 特殊要求，见下 |
| `rate` | number\|null | 同类线索历史成交率百分比，`null` 表示样本不足 |
| `price` | number | 区域仓租均价（元/㎡/天），用于收益算式 |

派生字段（由 `L()` 工厂函数计算，不手写）：

```js
tier    // 规模档名称
basePts // 规模基准积分
pts     // 实际消耗积分 = basePts × 质量系数（四舍五入）
fee     // 预估收佣 = round(area × 39.36)
```

`ai` 对象结构：

```js
ai: {
  real: ['高亮短语', '其余说明'],   // 真实性，第 0 项渲染为绿色高亮
  use:  '用途',
  hard: '硬性条件',
  area: '区域偏好',
  urg:  ['高/中/低', '说明'],       // 第 0 项渲染为橙色高亮
  todo: ['高亮短语', '其余说明']    // 待确认项
}
```

`spec` 数组每项为 `[字段名, 值, 标记]`，标记取值：

- `'src'` → 绿色徽标「来自通话」
- `'ask'` → 琥珀徽标「建议首次沟通确认」
- `'warn'` → 红色徽标「风险项」
- `''` → 无徽标

**14 条线索清单**（关键字段）：

| id | 城市 | 区域 | 类型 | 面积 | 用途 | 等级/分 | 积分 | 收佣 | 匹配 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| 215301 | 上海 | 普陀 | 仓库 | 100 | 可回收物中转站 | A 92 | 65 | 3,936 | 88% | **主线索**，含真实逐字稿 |
| 215288 | 上海 | 嘉定 | 仓库 | 100 | 仓储 | B 71 | 50 | 3,936 | 41% | 租赁要求空白 |
| 215262 | 上海 | 青浦 | 仓库 | 100 | 仓储放食品 | C 48 | 30 | 3,936 | 0 | 未核实，上架 6 天 |
| 215420 | 上海 | 不限 | 仓库 | 50 | 放不锈钢（短租一个月） | C 31 | 30 | 1,968 | 0 | **真实劣质线索样本** |
| 215194 | 上海 | 徐汇 | 仓库 | 300 | 存放医疗器械 | A 89 | 130 | 11,808 | 63% | 有逐字稿 |
| 215177 | 上海 | 浦东 | 厂房 | 300 | 棋牌室 | B 66 | 100 | 11,808 | 22% | 用途合规风险 |
| 215102 | 上海 | 松江 | 仓库 | 2000 | 电商仓配 | A 90 | 260 | 78,720 | 51% | 超大客户档 |
| 215088 | 上海 | 宝山 | 仓库 | 800 | 快递分拨 | B 74 | 100 | 31,488 | 35% | 已认领 2 席 |
| 214977 | 苏州 | 昆山 | 厂房 | 100 | 小加工 | B 68 | 50 | 3,936 | 74% | |
| 214901 | 苏州 | 吴中 | 仓库 | 1500 | 家具仓储 | A 87 | 260 | 59,040 | 18% | 有逐字稿 |
| 214860 | 苏州 | 相城 | 仓库 | 400 | 五金存放 | C 52 | 60 | 15,744 | 0 | |
| 214812 | 南京 | 雨花台 | 仓库 | 600 | 放黄沙 | B 70 | 100 | 23,616 | 0 | |
| 214790 | 南京 | 建邺 | 厂房 | 4000 | 做网球场 | C 45 | 120 | 157,440 | 0 | 用途存疑 |
| 214733 | 南京 | 栖霞 | 仓库 | 150 | 办公和仓储 | A 86 | 65 | 5,904 | 0 | 有逐字稿 |

> **手机号一律为合成号码。** 215301 原本沿用了线上真实号码，与同页的真实通话逐字稿
> （含姓氏、区域、行业）叠加后可识别到具体个人，公开托管前已替换为 `150 0000 0000`。
> 其余 13 条本就是编造号码。后续补充线索时同样不得写入真实联系方式。

> **215420 必须保留。** 它是真实线上线索：50 平、区域填"不限"、短租一个月、无微信无公司信息，
> 却按公式显示 ¥1,968 收佣。它与 215301 同属普通客户档，是"同档不同质"最直接的证据。

### 3.4 区域清单

```js
DISTRICTS = {
  上海: ['全部','普陀','嘉定','青浦','徐汇','浦东','松江','宝山','不限'],
  苏州: ['全部','昆山','吴中','相城'],
  南京: ['全部','雨花台','建邺','栖霞'],
}
```

### 3.5 通话逐字稿 TRANSCRIPT

数组，每项 `[speaker, text]`，`speaker` 为 `1`（平台客服）或 `0`（客户）。
渲染时客服行加 `.me` 类降低不透明度。共 26 轮，取自线上真实通话记录。

---

## 4. 业务规则

### 4.1 定价公式（本方案的核心改动）

```
规模档：area ≤ 200        → 普通客户，基准 50 积分
       201 ≤ area ≤ 1000 → 大客户，  基准 100 积分
       area > 1000        → 超大客户，基准 200 积分

质量系数：A 级 × 1.3　B 级 × 1.0　C 级 × 0.6

实际消耗 = round(基准 × 质量系数)
```

设计意图：**保留平台现有的规模三档，只在其上叠加缺失的质量维度。**
规模决定"成交了能赚多少"，质量决定"能不能成交"——现行定价只算了前者。

### 4.2 预估收佣

```
fee = round(area × 39.36)
```

线上真实公式，**刻意保留其缺陷**（不含区位、租期、成交概率），因为它是论证材料。
详情页的收益算式则展示"应该怎么算"：`面积 × 区域均价 price × 30 × 50%`。

### 4.3 认领席位

- 每条线索 `seats = 3`
- 展示为「限 3 人认领 · 剩 N 席」
- `taken >= seats` 时按钮显示"已满席"且不可点
- 认领成功 `taken += 1`；申诉退单 `taken -= 1`（不低于 0）

### 4.4 时效衰减

- `days >= 3` 的 C 级线索，积分角标显示删除线原价：`<s>50</s>30 积分`
- `days >= 3` 时列表底部追加琥珀色提示「积分随时效递减」
- 详情页的衰减说明分两种文案：
  - `days === 0`：说明初始价的构成（规模档 × 质量系数），强调越早认领期望收益越高
  - `days > 0`：说明降价原因是**成交概率随时间下降，而非促销**

### 4.5 积分收支

| 事件 | 变动 |
|---|---|
| 初始余额 | 190 |
| 认领线索 | `- lead.pts` |
| 回填「已联系」 | +5 |
| 回填「已带看」 | +20 |
| 回填「已成交」 | +200 |
| 回填「已战败」 | +5 |
| 申诉退单成功 | `+ lead.pts` |
| 充值 ¥99（首充） | +500 |
| 充值 ¥500 | +550 |
| 充值 ¥1000 | +1150 |
| 充值 ¥1500 | +1800 |

**战败同样给分**是刻意设计：质量分模型最需要的是负样本，而现行「上传成交 +200～500 积分」只收正样本。

### 4.6 积分价值换算（用于横向对标）

按 ¥500 档（赠 10%）折算，约 **¥0.91 / 积分**。据此：

- 普通客户线索 ≈ ¥45
- 大客户线索 ≈ ¥91
- 超大客户线索 ≈ ¥182
- 对照：汽车垂媒线索采买成本 200–500 元/条；2024 年折算到单条**成交**线索已达 1,580 元

结论：**问题不是价格太高，而是质量不可辨。** 质量可辨后 A 级线索有提价空间。

> 汽车行业的数据同时构成本方案最重要的外部证据：CPL 十年变化有限，
> 而转化率从 2019 年的 5% 降至 2025 年的 1.5%–2%，单条成交成本因此翻了数倍。
> 挂牌价与成交概率脱钩的终局就是这条曲线。来源与检索时间见分区 01 与页脚。

### 4.7 筛选与排序

筛选条件（可叠加）：城市（单选）、区域（单选）、类型（单选）、仅看匹配我的仓源（开关）、仅看已核实（开关）

排序：

| key | 名称 | 规则 |
|---|---|---|
| `match` | 匹配度（默认） | `match` 降序，同值按 `score` 降序 |
| `fresh` | 最新 | `days` 升序，同值按 `score` 降序 |
| `pts` | 积分低到高 | `pts` 升序 |
| `fee` | 收佣高到低 | `fee` 降序 |

- 切换城市时 `district` 重置为 `'全部'`
- 结果为空时显示空状态页，含「重置筛选条件」按钮
- 「重置筛选」恢复：区域全部、类型全部、两个开关关闭、排序回 `match`

---

## 5. 状态与路由

### 5.1 状态对象

```js
const DEF = () => ({
  pts: 190,
  city: '上海', district: '全部', type: '全部', sort: 'match',
  onlyMatch: false, onlyVerified: false,
  stack: [{ r:'home' }],      // 导航栈
  claimed: {},                 // { [leadId]: { status: 0-4 } }
  subs: [],                    // 已订阅行政区，元素形如 '上海普陀'
  subStores: [],               // 已订阅仓源 id
  onboard: 0,                  // 0=关闭，1/2/3=引导第 N 步
  notes: false,                // 说明面板开关（默认收起，仅显示「可以试试」清单）
  trOpen: false,               // 逐字稿展开状态
  packIdx: 1,                  // 充值档位选中项
});
```

> `tab` 字段当前未被使用，重构时应删除。

### 5.2 导航

```js
cur()        // 栈顶路由
go(r, p)     // 入栈，重置 trOpen
back()       // 出栈（栈内仅剩 1 项时不动作）
resetTo(r)   // 清空栈并置为单个路由
```

路由取值：`home` `mall` `detail` `claimed` `mine` `points` `recharge` `subscribe`
带参路由（`detail` / `claimed`）额外携带 `id`。

**关键行为**：认领确认后先 `stack.pop()` 弹出 detail，再 `go('claimed')`。
这样从认领成功页返回时回到列表，而不是回到已消费的详情页。

### 5.3 底部 tabBar 映射

| tab | 图标 | 激活于哪些路由 |
|---|---|---|
| `home` 首页 | 🏠 | home |
| `mall` 线索商城 | 🏷 | mall, detail |
| `mine` 我的客户 | 👥 | claimed, mine |
| `me` 我的 | 👤 | points, recharge, subscribe |

点击 `me` 跳转至 `points`。

### 5.4 顶部导航栏

- `home` 不渲染导航栏
- `claimed` / `points` / `recharge` 为**蓝底导航栏**（`.onblue`），背景色与该页 hero 顶部同色，文字白色，**不显示积分角标**
  - `claimed` → `#3D5AFE`；`points` / `recharge` → `#4C6EF5`
- 其余为白底导航栏，右侧显示积分角标（可点击跳转积分页），`margin-right: 98px` 避让微信胶囊
- 状态栏背景与导航栏一致；蓝底页面状态栏文字转白（`.light`）
- 栈深度 > 1 时显示返回箭头

---

## 6. 页面规格

### 6.1 首页 `home`

| 区块 | 内容 |
|---|---|
| hero | 蓝色渐变，品牌区（logo「仓」+ 头等仓 + 工业及物流地产获客平台）+ 两个入口卡片 |
| 入口卡片 | 左「找客户 / 线索商城 / 3,800 个新客户急租厂房仓库 / 近 7 天新增 223」；右「管房子 / 租控表 / 一张表管完所有房源 / 全城 3,000 位经纪人帮你找客」 |
| **为你匹配**（新增） | 显示"你有 6 个仓源 · N 条需求匹配"，列出 2 条匹配度最高的线索（等级角标 + 标题 + 匹配仓源名 + 匹配度），底部「查看全部匹配需求」 |
| 快捷入口 | 我的积分 / 订阅线索 / 我的客户，各显示当前数值 |
| 各城市在售线索 | 三行，每行城市名 + 总数 + 三条线索标题，点击切换到该城市列表 |

租控表卡片点击仅弹 toast「租控表为现有功能，本原型聚焦线索商城」。

### 6.2 线索商城 `mall`

**筛选区**（四排 chip，横向可滚动）：

1. 城市：上海 / 苏州 / 南京 / 全国（"全国"仅弹 toast 说明）
2. 区域：随城市变化
3. 类型：全部 / 厂房 / 仓库 ＋ `⌂ 匹配我的仓源`（绿色态）＋ `✓ 仅看已核实`
4. 排序四项 ＋ `🔔 订阅推送`（跳订阅页）

**计数行**：`{城市}共 N 条 · M 条匹配你的仓源`，右侧「重置筛选」

**线索卡片**结构（自上而下）：

1. 等级角标（字母 + 分数）＋ 标题「区域 · **面积** · 用途」（面积橙色）
2. 标签行：已电话核实/未核实、通话凭证、需求紧急、非中介、规模档、信息完整度
3. 描述（最多两行，超出省略；`complete < 50` 时文字转浅）
4. 匹配条：环形进度 + 「匹配你的 N 个仓源：仓源名」，无匹配时显示「你在 XX 暂无匹配仓源」
5. 底部：预估收佣 + 同类成交率（`rate` 为 null 时显示「成交率样本不足」）｜ 积分角标 ＋ 认领按钮
6. 时效行：上架时间 · 查看人数 · 限 N 人认领剩 M 席 ·（≥3 天）积分随时效递减

已认领的卡片整体降透明度，按钮显示「已认领」；满席显示「已满席」。

### 6.3 需求详情 `detail`

| 区块 | 说明 |
|---|---|
| 头部 | 等级角标 + 大标题 + 手机号（未认领时中间四位打码）+ 状态行（查看人数、核实状态、上架时间、剩余席位） |
| **AI 通话摘要卡** | 仅 `record:true` 时显示。蓝色渐变卡片，六行：真实性 / 用途 / 硬性条件 / 区域 / 紧急度 / 待确认。底部按钮展开完整逐字稿（最大高度 200px 内滚动） |
| **无凭证告警** | `record:false` 时替代摘要卡，红色左边框，说明未经二次确认及退单权益 |
| 需求信息 | 类型 / 用途 / 面积 / 区域 / 描述 |
| 特殊要求 | 按 `spec` 渲染，带来源徽标。标题右侧标注数据来源 |
| 收益预估 | 橙色卡片。大金额 + 四行算式：面积×均价、佣金比例、历史成交率、期望收益÷积分。标题右侧「怎么算？」弹 toast 说明现行公式的缺陷 |
| 时效说明 | 琥珀条，文案随 `days` 变化（见 4.4） |
| 权益保障 | 三格：平台电话核实 / 不真实退积分 / 打不通退积分 |
| dock | 未认领：左侧「消耗 N 积分」+ 主按钮；已认领：「查看客户与跟进进度」；满席：禁用按钮 |

### 6.4 认领成功与跟进 `claimed`

| 区块 | 说明 |
|---|---|
| hero | 蓝色，对勾图标 + 「认领成功」+ 线索标题 + 消耗与余额 |
| 联系卡 | 完整手机号（大字号）+ 一键拨号 / 复制微信号（均为 toast） |
| 首次沟通建议 | 仅有 `ai` 时显示。开场话术（215301 用专属文案，其余用模板）+ 琥珀色「务必确认」列出 `spec` 中标记为 `ask` 的字段 |
| 跟进进度 | 四步进度条（已认领→已联系→已带看→已成交）。战败状态时后三步显示为红色「—」 |
| 战报回填 | 四个按钮，各带积分奖励。点击后加分、更新状态、弹 toast。重复点击同一状态提示「该状态已回填」 |
| 申诉 | 红色条，「24 小时内可申请退回 N 积分」+ 去申诉按钮 |
| 匹配仓源 | 有匹配时列出仓源名/面积/层高/匹配度 + 「生成房源卡片并发送」；无匹配时提示去园区字典找房 |
| dock | 「继续挑选线索」 |

### 6.5 我的客户 `mine`

- 空态：图标 + 提示 + 「去线索商城」按钮
- 列表：标题 + 状态徽标（五色）+ 手机号/消耗积分/等级分
- 顶部一行说明「共 N 位客户 · 回填进展可获积分」

### 6.6 我的积分 `points`

- 蓝色 hero 显示大数字余额
- **如何获取积分**：首次登录 +100（已发送）、订阅客户日报 +100、上传成交 +200、回填跟进进展 +5、积分充值
- **积分如何消耗**（改版新增）：三档基准 + 质量系数说明，并解释为什么要有质量系数

### 6.7 充值中心 `recharge`

- 首充卡片（¥99 → 500 积分）+ 三档（¥500/¥1000/¥1500，赠 10%/15%/20%）
- 换算区：到账积分、折合单价、可认领各档条数
- **横向参照**（改版新增）：三档折合人民币 vs 汽车行业 200–300 元/条，并给出「问题不是价格太高，而是质量不可辨」的结论
- dock：「充值 ¥X · 到账 Y 积分」，点击真实到账

### 6.8 订阅线索 `subscribe`

- 浅蓝头部
- **按我的仓源订阅**（改版新增，蓝色左边框）：6 个仓源可勾选，说明按面积区间、层高、区位自动比对
- **按行政区订阅**（线上已有）：当前城市的区域网格，说明颗粒度较粗
- dock：显示已选数量 + 保存按钮（未选时提示，保存后 toast 并返回）

### 6.9 新手引导浮层

三步，覆盖手机全屏，底部卡片式：

1. **看懂线索卡片** —— A/B/C 三级质量分的含义
2. **用积分认领** —— 积分四种获取途径 + 消耗规则（规模三档 × 质量系数）
3. **打不通可退** —— 三步退单路径，并说明申诉会被记为质量反馈

首次加载自动显示第 1 步。可跳过、可通过手机下方按钮重看。

---

## 7. 交互动作清单

全部通过 `document` 上的事件委托实现，用 `closest('[data-*]')` 匹配。

| data 属性 | 作用 |
|---|---|
| `data-go` (+`data-id`) | 入栈跳转 |
| `data-back` | 返回 |
| `data-tab` | 切换底部 tab（`me` → points） |
| `data-city` / `data-dist` / `data-type` | 设置筛选 |
| `data-tog` | 切换布尔筛选开关（值为状态字段名） |
| `data-sort` | 设置排序 |
| `data-reset` | 重置筛选条件 |
| `data-claim` | 打开认领确认弹层（积分不足则打开充值引导层） |
| `data-doclaim` | 执行认领 |
| `data-close` | 关闭弹层 |
| `data-torecharge` | 关闭弹层并跳充值 |
| `data-rep` | 回填战报（值为状态索引 1–4） |
| `data-appeal` | 打开申诉弹层 |
| `data-doappeal` | 执行退单（**独立的第二个监听器**） |
| `data-sub` / `data-sub-store` | 切换订阅项 |
| `data-savesub` | 保存订阅 |
| `data-pack` / `data-buy` | 选择充值档位 / 执行充值 |
| `data-ptask` | 积分页任务跳转 |
| `data-tr` | 展开/收起逐字稿 |
| `data-toast` | 弹出提示（值即文案） |
| `data-notes` | 切换说明面板 |
| `data-coach` | 设置引导步骤（0 为关闭） |
| `data-resetall` | 重置整个原型（同时还原各线索 `taken` 初值） |
| `data-sec` / `data-route` | 顶部分区导航 |

点击 `.backdrop` 关闭弹层。

---

## 8. 说明面板

位于手机右侧，**内容随当前路由自动切换**，由 `NOTES` 字典驱动。

每个路由的说明包含：

```js
{
  t: '标题',
  l: '导语（支持 <b> 高亮）',
  callout: ['小标题', ['段落1','段落2']],    // 可选，橙色左边框强调块
  c: [                                        // 改动点列表
    ['序号', '改动标题', [
      ['改前', '内容', 'was'],   // 第三项为 'was' 时文字转灰
      ['改后', '内容', ''],
      ['依据', '内容', ''],
    ]]
  ],
  try: ['可以试试的操作1', '操作2']            // 蓝色提示框
}
```

顶部有「设计说明：开/关」切换按钮。关闭时仅保留"可以试试"清单。

**这个面板是交付物的一部分，不是调试辅助。** 因为交付时无人讲解，说明必须自己说话。

---

## 9. 分析屏内容清单

四个全宽分析屏，纯静态内容，无交互（除内部无跳转）。

| 屏 | 主要内容 |
|---|---|
| **00 业务结构** | 三方角色卡（租户/业主/经纪人）、线索流转 SVG 图、「线索由平台自己生产」论证、行业特性表（5 行）、双边结构对照（免费侧/收费侧）、两个张力、三个改版前提 |
| **01 用户定位** | 四象限 SVG（仓源密度 × 展业频率）、四类人群分层表（含 ROI 容忍度）、价值三因子（可转化性/独占性/及时性）、核心矛盾（批发市场 vs 准会员制）、归因缺失论证、待内部确认的三个问题、**外部数据校准表（查得到/查不到）与同构行业线索漏斗基准表** |
| **06 改版对比** | 现状/改版后对照卡（各 9 条）、漏斗七环预期影响表、「认领率不应作为北极星」论述、跨行业参照表、长期方向（从卖线索到分佣金） |
| **07 落地方案** | P0/P1/P2 分期依赖表、指标口径与埋点表（8 项）、质量分冷启动权重表（5 维度）、实验设计三卡、定价横向校准表、最大落地风险与两步走应对 |

内容以现有实现为准，重构不得删改文案。

---

## 10. 已知实现细节与坑

重构时必须保留下列行为，否则会引入回归：

1. **导航条不可用 sticky + 渐变遮罩**——曾导致第二排按钮被遮住。
2. **`.tabbar` 与 `.navtabs` 是两个不同的东西**，前者在手机内，后者在页面顶部。
3. **分析屏的表格样式必须带 `.analysis` 前缀**，否则会污染手机内的表格。
4. **`.sec-h`（手机内）与 `.an-h`（分析屏）不可合并。**
5. **线索卡片是 `<button>`，内部的"认领"是 `<span>` 而非嵌套按钮**，避免非法嵌套。
6. **`data-doappeal` 由独立的第二个事件监听器处理**，因为它不在主监听器的选择器列表内。
7. **认领后先 `stack.pop()` 再 `go('claimed')`**，保证返回路径正确。
8. **`visibleLeads()` 内的 `.sort()` 作用于 `filter` 产生的新数组**，不会污染 `LEADS` 原始顺序。
9. **`lead.taken` 会被认领/退单直接修改**，`data-resetall` 必须手动还原其初值
   （215301→1、215194→1、215088→2、214901→1，其余→0）。
10. **禁止任何浏览器存储**，刷新即回到初始状态是预期行为。
11. **移动端适配（易踩坑，务必保留）**
    - `<meta name="color-scheme" content="dark">` 与 `:root{color-scheme:dark}` **不可删除**。
      缺少这个声明时，Android Chrome 的自动深色模式会强制反转页面里的浅色区域——
      外壳本就是深色不受影响，而手机原型内部是白底，会被整片反转成黑色。
      这是真机上出现过的实际问题，桌面端完全复现不出来。
    - `.screen` 上的 `color-scheme:light` 与 `forced-color-adjust:none` 是第二道保险。
    - `html,body{overflow-x:hidden}` 防止手机外框（390 + 22 = 412px）在窄屏横向溢出。
    - 窄屏用 `transform: scale()` 缩放手机，并用**负 margin-bottom 抵消缩放后的高度空白**
      （缩放不改变布局盒尺寸，不补偿会留下大片空白）。三档断点与补偿值：

      | 断点 | scale | margin-bottom | 手机实占宽 |
      |---|---|---|---|
      | ≤ 460px | 0.86 | −112px | 354px |
      | ≤ 400px | 0.78 | −176px | 321px |
      | ≤ 360px | 0.70 | −240px | 288px |

      补偿值 = 800 × (1 − scale)。改 scale 必须同步改 margin-bottom。
    - ≤760px 时 `.roles` 与 `.cmp` 转为单列，`.wrap` 内边距收窄至 14px。

---

## 11. 本次任务：原样重构

### 11.1 范围

**只做代码组织的整理，不改任何可见行为、不加任何功能、不改任何文案。**

允许并鼓励：

- [ ] 在单文件内按清晰分区组织代码，加分隔注释：`常量 / 数据 / 状态 / 计算 / 片段 / 页面 / 导航 / 交互 / 分区 / 初始化`
- [ ] 抽取重复的魔法值为具名常量（如席位数 3、积分价 0.91、各档充值配置、初始 `taken` 映射）
- [ ] 统一命名风格（当前混用 `l`/`lead`、`c`/`cur()`、`d`/`dataset`，应统一）
- [ ] 合并重复的样式规则，按「基础 / 布局 / 手机组件 / 分析屏 / 覆盖层」分区并加注释
- [ ] 把内联 `style="..."` 中反复出现的样式提取为类
- [ ] 消除 `pageClaimed` 与 `pageDetail` 中重复的标题渲染逻辑
- [ ] 删除未使用的状态字段 `tab` 与未使用的 CSS 规则
- [ ] 为主要函数加简短的用途注释（一行即可，不要写 JSDoc 长注释）
- [ ] 修正任何明显的可访问性问题（缺失的 `aria-label`、按钮语义），前提是不改变外观

**禁止**：

- 拆分为多个文件
- 引入构建工具、框架或任何外部依赖
- 改动数据（14 条线索、6 个仓源、逐字稿内容）
- 改动任何界面文案或说明面板文案
- 改动业务规则或数值
- 改变视觉呈现

### 11.2 验收清单

重构完成后逐项走一遍，全部通过才算完成：

**导航**
- [ ] 8 个顶部分区按钮两排完整显示，无遮挡，默认选中 02
- [ ] 00/01/06/07 显示分析屏并隐藏手机；02–05 显示手机并落到对应界面
- [ ] 05 在未认领时会自动认领 215301 并落到跟进页
- [ ] 手机底部四个 tab 可切换，高亮状态正确
- [ ] 详情页返回回列表；认领成功页返回不回到详情页

**筛选**
- [ ] 切换城市，线索数变为 上海 8 / 苏州 3 / 南京 3
- [ ] 切换城市后区域筛选自动重置为「全部」
- [ ] 「普陀 + 厂房」组合进入空状态页
- [ ] 「仅看匹配我的仓源」筛出 6 条
- [ ] 四种排序均生效
- [ ] 「重置筛选」恢复默认

**认领**
- [ ] 点认领弹出确认层，层内正确显示「规模基准 × 质量系数 = 消耗」
- [ ] 确认后积分扣减、席位减一、跳转认领成功页、弹 toast
- [ ] 积分不足时弹出充值引导层而非直接扣减
- [ ] 已认领的卡片降透明度且按钮显示「已认领」
- [ ] 215088 显示「剩 1 席」（初始 taken=2）

**跟进与退单**
- [ ] 四个回填按钮分别加 5/20/200/5 积分并更新进度条
- [ ] 重复点击同一状态提示「该状态已回填」
- [ ] 「我的客户」中状态同步
- [ ] 申诉退单后积分退回、席位释放、客户从列表移除

**其他**
- [ ] 充值四档切换，换算数字实时变化，充值后真实到账
- [ ] 订阅可勾选仓源与行政区，计数正确，保存后返回
- [ ] 逐字稿可展开收起
- [ ] 新手引导三步可前进后退跳过，可通过手机下方按钮重看
- [ ] 「重置原型」恢复全部状态，包括各线索的 `taken` 初值
- [ ] 说明面板随界面切换，开关可用
- [ ] 蓝底页面（认领成功/积分/充值）导航栏与状态栏同色，中间无白条
- [ ] 控制台无报错
- [ ] 单文件、无外部请求、无 localStorage

**移动端（必须用真机验证，桌面端复现不出来）**
- [ ] Android Chrome 打开，手机原型内部**仍是白底**，未被自动深色模式反转
- [ ] 页面无横向滚动条
- [ ] 手机外框完整可见，未被裁切
- [ ] 手机下方无因缩放产生的大片空白
- [ ] iOS Safari / 微信内置浏览器同样正常

### 11.3 交付

产出一个 `index.html`，可直接双击打开或部署到静态托管。
同时在 PR 描述或输出中简述做了哪些整理、删了哪些死代码。

---

## 12. 后续变更流程

1. 在对话中先讨论清楚要改什么、为什么改
2. 产出一段增量说明（追加到本文件的对应章节，或单独的 `CHANGE-xxx.md`）
3. 交给 Claude Code 执行
4. 用第 11.2 节的验收清单 + 本次变更的新增验收项回归

**本文件与代码必须同步更新。** 改了代码没改 SPEC，下次就会踩坑。
